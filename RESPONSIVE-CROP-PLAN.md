# 반응형 방식 전환 계획 (스케일 → 크롭)

> 2026-09-10 대화에서 결정된 사항을 기록한 문서. **아직 구현 안 됨** — 모바일
> 버전 연동을 먼저 끝낸 뒤, 시간 날 때 이 문서 기준으로 작업할 것.

## 배경

지금(이 문서 작성 시점) 코드는 `/main/parts`, `/main/furniture`, `/info`,
TopBar, MainSideNav의 `fixed` variant가 전부 `min(1, 화면폭/1920)` 비율로
**요소 자체를 축소/확대**하는 방식이다(`.figma-canvas-*`, `.figma-fixed-scale`,
globals.css). `/intro`, `/caption/[slug]`는 `useContainScale`로 가로+세로
둘 다 고려해 축소한다. 이 방식은 "화면이 좁아지면 텍스트/이미지가 다 같이
작아진다"는 문제가 있고, 다음 세 가지 새 규칙으로 대체하기로 했다.

## 확정된 규칙

1. **뷰포트를 줄이면 요소 크기는 그대로, 오른쪽에서 크롭된다.**
   왼쪽(x=0)은 항상 고정 기준점. 화면이 1920px보다 좁아지면 그만큼 오른쪽
   내용이 뷰포트 밖으로 나간다 — 단, `overflow: hidden`으로 완전히 숨기지
   않고 **가로 스크롤(`overflow-x: auto`)로 접근 가능**하게 한다. 즉
   TopBar의 `furniture`/`parts` 내비게이션, 그리드 오른쪽 타일들도 스크롤하면
   여전히 보이고 클릭할 수 있어야 한다.

2. **스크롤 없는 페이지(`/intro`, `/caption/[slug]`)도 같은 원칙 — 위/아래
   크롭 허용.** 지금처럼 `useContainScale`로 가로+세로를 다 축소해서 한
   화면에 욱여넣지 않고, 마찬가지로 실제 크기 그대로 두고 화면보다 크면
   위/아래(또는 좌우)가 잘리는 걸 허용한다. 다만 이 두 페이지는 원래
   스크롤이 없는 게 디자인 의도이므로, 잘렸을 때 사용자가 나머지를 어떻게
   보게 할지(세로 스크롤을 새로 허용할지, 아니면 정말 안 보이게 둘지)는
   페이지별로 결정 필요 — **미결 사항**, 아래 참고.

3. **요소 자체를 직접 축소/확대하는 CSS(`transform: scale()`, cqw 기반 계산
   등)는 최대한 쓰지 않는다.** 모든 요소는 Figma의 실제 px 값 그대로
   렌더링되고, 화면 크기에 따라 "보이는 영역"만 달라진다.

4. **브라우저 배율(줌)은 별도 처리 안 함(A안).** 위 1~3번대로 스케일 없이
   순수 px 고정 + 크롭 구조로 가면, 브라우저 줌은 자연스럽게 "보이는 영역이
   넓어지거나 좁아지는" 정도로만 작동한다 — 지금처럼 배율 공식과 상쇄되어
   확대해도 화면이 안 커지는 문제 자체가 없어진다. 별도 JS/CSS 대응 불필요.

## 영향 범위

| 대상 | 지금 방식 | 바뀔 방식 |
|---|---|---|
| TopBar (전역) | `.figma-fixed-scale` (transform scale) | 리터럴 px 그대로, 스케일 제거 |
| MainSideNav `fixed` | `.figma-fixed-scale` | 리터럴 px 그대로, 스케일 제거 |
| `/main/parts`, `/main/furniture` | `.figma-canvas-*` (transform scale) | 1920px 고정 폭 컨테이너 + `overflow-x: auto`로 오른쪽 크롭/스크롤 |
| `/info` | `.figma-canvas-*` | 위와 동일 |
| `/intro` | `useContainScale` (가로+세로 축소) | 1920×1080 고정, 화면보다 작으면 크롭 — 앵커/스크롤 여부 미결 |
| `/caption/[slug]` | `useContainScale` | 위와 동일 — 앵커/스크롤 여부 미결 |
| `.figma-pin` / `createFigmaGeom` (`figma-layout.ts`) | 이미 죽은 코드(아무 데서도 안 씀) | 이번 작업 때 같이 정리 대상 |

## 미결 사항 (구현 전 결정 필요)

- **`/intro`, `/caption` 세로 크롭 앵커**: 지금 `/intro`는 구성 요소가
  프레임 정중앙에 배치돼 있고(center-anchored), `/caption`은 top-left
  앵커다. 화면이 1080px보다 낮을 때 위/아래 중 어느 쪽을 자를지, 아니면
  세로 스크롤을 새로 허용할지 페이지별로 정해야 함.
- **가로 스크롤 시각적 힌트 필요 여부**: 스크롤 가능하다는 걸 사용자가
  알아채도록 스크롤바를 항상 보이게 할지, 그림자/화살표 같은 힌트를 줄지.
- **`/intro`의 드래그 인터랙션**: 지금은 배율(scale)로 나눠서 포인터 이동량을
  보정하는데(`intro/page.tsx`의 `deltaCanvas = deltaScreen / scale`), 스케일이
  없어지면 이 보정 자체가 필요 없어짐 — 단순화됨.

## 우선순위

1. **지금 먼저**: 모바일 전용 Figma 디자인 연동 (768px 미만, 페이지별로 링크
   받는 대로 순차 진행) — 이 문서와 무관하게 진행.
2. **그 다음, 시간 될 때**: 위 규칙대로 데스크톱 반응형 방식을 스케일 →
   크롭으로 전환.
